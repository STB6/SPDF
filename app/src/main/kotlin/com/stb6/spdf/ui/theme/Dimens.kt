package com.stb6.spdf.ui.theme

import androidx.compose.ui.unit.dp

object Dimens {

    /** Excludes the status bar; content and padding must fit this height to match the body inset. */
    val TopBarHeight = 56.dp

    val TopBarStartPadding = 16.dp

    val TopBarEndPadding = 4.dp

    /** Keep icons above the transparent edge of the top-bar scrim. */
    val TopBarScrimTail = 14.dp

    val TopBarIconSize = 40.dp

    val ContentHorizontalPadding = 16.dp

    val ContentVerticalPadding = 12.dp

    val SettingsHorizontalPadding = 24.dp

    val SettingsItemGap = 4.dp

    val SettingsRowMinHeight = 48.dp

    val SettingsMaxWidth = 600.dp

    val SectionGap = 20.dp

    val SettingsMultiLineMargin = 12.dp

    val ContentMaxWidth = 400.dp

    val TapCornerRadius = 12.dp

    val TapBleed = 8.dp

    const val DisabledAlpha = 0.38f
}
